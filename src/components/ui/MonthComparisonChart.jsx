import { Bar, BarChart, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { useTranslation } from "react-i18next";
import "../../styles/components/MonthComparisonChart.css";
function MonthComparisonChart({ transacoes }) { const { t } = useTranslation(); const now = new Date(); const current = now.getMonth(); const previous = current === 0 ? 11 : current - 1; const year = now.getFullYear(); const categories = ["ALIMENTACAO", "TRANSPORTE", "LAZER", "ESTUDOS", "OUTROS"]; const total = (category, targetYear, targetMonth) => transacoes.filter((item) => item.tipo === "SAIDA" && item.categoria === category && item.data && new Date(`${item.data}T00:00:00`).getFullYear() === targetYear && new Date(`${item.data}T00:00:00`).getMonth() === targetMonth).reduce((sum, item) => sum + Number(item.valor), 0); const totals = categories.map((category) => ({ name: t(`categories.${category}`), current: total(category, year, current), previous: total(category, year - (current === 0 ? 1 : 0), previous) })); return <section className="month-comparison"><span className="chart-kicker">{t("charts.trend")}</span><h2>{t("charts.monthComparison")}</h2><ResponsiveContainer width="100%" height={280}><BarChart data={totals}><CartesianGrid strokeDasharray="3 3" stroke="#2b3955" /><XAxis dataKey="name" stroke="#94a3b8" /><YAxis stroke="#94a3b8" /><Tooltip /><Legend /><Bar dataKey="current" name={t("charts.current")} fill="#4f8cff" radius={[5, 5, 0, 0]} /><Bar dataKey="previous" name={t("charts.previous")} fill="#64748b" radius={[5, 5, 0, 0]} /></BarChart></ResponsiveContainer></section>; }
export default MonthComparisonChart;
